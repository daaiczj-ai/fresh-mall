{
  "name": "fresh-mall",
  "version": "1.0.0",
  "description": "社区生鲜店微信小程序 - 同城配送与到店自提",
  "private": false,
  "workspaces": [
    "server",
    "admin"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:admin": "npm run dev --workspace=admin",
    "install:all": "npm install && cd miniprogram && npm install"
  }
}
