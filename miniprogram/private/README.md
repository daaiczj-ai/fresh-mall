# 小程序代码上传密钥

将从微信公众平台下载的 `.key` 文件放在本目录，例如：

```
private.wx你的AppID.key
```

## 获取方式

微信公众平台 → 开发 → 开发管理 → 开发设置 → 小程序代码上传 → 生成 / 下载

## 使用方式

微信开发者工具 → 点击 **上传** → 首次会提示选择本目录下的 `.key` 文件。

> 此目录下的 `*.key` 已加入 `.gitignore`，请勿提交到公开仓库。
